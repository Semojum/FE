// 아직 완성되지 않은 기능 — 왜 안 되는지 알린다.
//
// 3.3.0에서 1차 PoC 요청을 화면까지 만들어 두었지만, **화면에서만 되고 결과물에는
// 반영되지 않는 것**이 여럿이다. 조판 설정을 40칸으로 바꾸고 작업했는데 내려받은
// .brf가 32칸으로 나오면, 점역사는 그것을 마지막에야 안다.
//
// 한동안 그런 기능은 프로덕션에서 아예 막았다. 2026-09-03에 방침을 바꿨다 —
// **막는 대신 열고, 쓰는 자리에서 한계를 밝힌다.** 반쪽이라도 쓸 수 있는 편이 낫다는
// 판단이다. 대신 "되는 척"은 하지 않는다: 표식을 넣는 자리에서 한 번(쪽바꿈 토스트 ·
// SectionFooterModal), 다운로드 모달에서 표식 개수와 함께 한 번 더 알린다
// (DownloadModal `pendingMarks`).
//
// 그래서 여기 남는 것은 **쓰는 자리에서 알릴 데가 없는** 기능뿐이다 — 고르는 것만으로는
// 화면에 아무 일도 일어나지 않아 무엇이 빠졌는지 알 수가 없는 점역 옵션 같은 것.
// 각 항목의 근거는 docs/SERVER-REQUIREMENTS-3.3.0.md에 있다.
//
// 2026-09-01 명세 개정으로 조판 설정(S-1)·규정 검색(S-6)·원본 쪽 번호 시작이 열렸다.
// 2026-09-02 braille-assist 갱신으로 변경선 끄기(showChangeLine)와 꼬리말 우측
// 정렬이 열렸다 — 라이브러리가 옵션을 받게 되어 여기서 뺐다.
// 2026-09-03 표식 두 개(쪽바꿈 L-2 · 구간 꼬리말 L-3)를 뺐다. 파일에는 여전히 안
// 들어가지만 화면에서는 쓸 수 있게 열었다. (구간 꼬리말은 실은 문구만 있고 막는 코드가
// 없었다 — 처음부터 열려 있었다.)

export type UnfinishedId = 'translation';

export interface UnfinishedNotice {
  title: string;
  /** 무엇이 안 되는지 + 무엇을 기다리는지. 줄바꿈은 그대로 그려진다. */
  body: string;
}

export const UNFINISHED: Record<UnfinishedId, UnfinishedNotice> = {
  translation: {
    title: '점역 옵션은 아직 준비 중입니다',
    body:
      '점자 등급(1급/2급) · 영어 점자 · 한영 혼용 규정을 고르는 기능입니다.\n' +
      '고른 값이 아직 변환에 전달되지 않아, 무엇을 고르든 결과가 같습니다.\n\n' +
      '서버와 AI가 이 값을 받기 시작하면 열립니다.',
  },
};
